#include <stdio.h>
#include <locale.h>

int main ()
{
    while (1)
    {
        aprender();
        compartilhar();
        evoluir();
        if (morte())
            break;
    }
        return 0;
    }

